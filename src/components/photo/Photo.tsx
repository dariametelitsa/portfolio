import React from 'react';
import styled from 'styled-components';

type PhotoPropsType = {
}

export const Photo = () => {
  return (
    <div>
      
    </div>
  );
};

const StyledPhoto = styled.img `
  
`